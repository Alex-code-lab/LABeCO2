[pytest]
# docs/ contient des tests pédagogiques qui mockent PySide6 au niveau module.
# Ce mocking pollue la session pytest pour tous les tests suivants.
# On les exclut du discovery automatique ; ils restent exécutables via : pytest docs/
testpaths = tests
